<%= form_for(@musictrack, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @musictrack.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@musictrack.errors.count, "error") %> prohibited this musictrack from being saved:</h4>

      <ul>
      <% @musictrack.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
